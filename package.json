{
  "scripts": {
    "watch": "watch ./make.sh src/* def req --wait=20 --interval=10 -d -u"
  },
  "devDependencies": {
    "watch": "^0.19.1"
  },
  "dependencies": {
    "cartesian-product": "^2.1.2",
    "compute-unzip": "^1.0.1",
    "event-stream": "^3.3.4",
    "sprintf-js": "^1.0.3"
  }
}
