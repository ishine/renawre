{
  "scripts": {
    "build": "npm run buildone $(cd rtools/src-js; ls *.js)",
    "buildone": "rtools/src-js/buildjs.sh",
    "watch": "watch ./make.sh src/* def req --wait=20 --interval=10 -d -u"
  },
  "devDependencies": {
    "watch": "^0.19.1"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.13.2",
    "babelify": "^7.3.0",
    "browserify": "^13.1.0",
    "cartesian-product": "^2.1.2",
    "chinese-to-pinyin": "^0.1.3",
    "compute-unzip": "^1.0.1",
    "event-stream": "^3.3.4",
    "sprintf-js": "^1.0.3",
    "uglifyify": "^3.0.2"
  }
}
